function [x, y, z]=chop(A)
    [~, q]=size(A);
    l=length(A);
    if  q==1 && l==3
        x=A(1,1);
        y=A(2,1);
        z=A(3,1);
    else
        x=[0,0,0];
        y=0;
        z=0;
        fprintf('invalid input!\n');
        
    end
